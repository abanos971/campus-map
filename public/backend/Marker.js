class Marker { // gonna add more to this, more data and methods, but for now just a simple class to hold marker data
  constructor(name, lat, lng) {
    this.name = name;
    this.lat = lat;
    this.lng = lng;
  }
}

module.exports = Marker;